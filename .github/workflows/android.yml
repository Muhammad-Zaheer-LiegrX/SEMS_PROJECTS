name: Debug Repository

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Repository Structure
        run: |
          pwd
          echo "===================="
          ls -la
          echo "===================="
          find . -type f | sort
